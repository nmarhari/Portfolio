var typed = new Typed('.dynamic-text', {
    strings: ['code together', 'create together', 'innovate together', 'build together'],
    typeSpeed: 50,
    backSpeed: 50,
    backDelay: 2500,
    loop: true,
    smartBackspace: false,
});